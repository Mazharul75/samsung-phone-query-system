[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    llm: test runs the local language model (slower)
filterwarnings =
    ignore::DeprecationWarning
